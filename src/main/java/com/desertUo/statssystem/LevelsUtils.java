package com.desertUo.statssystem;

public class LevelsUtils {
    public void addExp(int amount) {
        // this.xp += amount;

        // Create the event object
        // PlayerExpChangeEvent event = new PlayerExpChangeEvent(this.player, amount, this.xp);

        // Fire the event so the Listener can hear it
        // Bukkit.getPluginManager().callEvent(event);
    }
}
